{
  "name": "example-basic",
  "type": "module",
  "scripts": {
    "dev": "vinxi dev",
    "build": "vinxi build",
    "package": "bun run build && mkdir -p dist && for pkg in pathe seroval seroval-plugins solid-js vinxi; do rm -rf .output/server/node_modules/$pkg && ln -s ../../../node_modules/$pkg .output/server/node_modules/$pkg; done && bun build --compile --outfile dist/app .output/server/index.mjs",
    "start": "vinxi start",
    "version": "vinxi version"
  },
  "dependencies": {
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.0",
    "@solidjs/start": "^1.1.0",
    "seroval": "^1.5.0",
    "seroval-plugins": "^1.5.0",
    "solid-js": "^1.9.5",
    "vinxi": "^0.5.7"
  },
  "engines": {
    "node": ">=22"
  }
}
